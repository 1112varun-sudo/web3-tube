# Web3 YouTube

This project contains a `VideoPlatform` smart contract, a deployment script, and a React client.

Useful commands:

```shell
npm run compile
npm run test
npm run deploy
cd client && cmd /c npm start
```
